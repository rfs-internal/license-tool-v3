@model IEnumerable<ICSLicenseMaintV2.License>

<table class="table table-condensed table-hover @(ViewBag.LicenseData == null ? "data" : "")">
    <thead>
        <tr>
            <th></th>
            <th class="hidden-xs" data-toggle="tooltip" data-placement="top" title="@LicenseMaintStrings.HelpLicenseID" >ID</th>
            <th class="hidden-xs" data-toggle="tooltip" data-placement="top" title="@LicenseMaintStrings.HelpMachineID">Machine ID</th>
            <th data-toggle="tooltip" title="@LicenseMaintStrings.HelpMachineName">Machine Name</th>
            <th class="hidden-xs" data-toggle="tooltip" data-placement="top" title="@LicenseMaintStrings.HelpUserCount">Users</th>
            <th class="hidden-xs" data-toggle="tooltip" data-placement="top" title="@LicenseMaintStrings.HelpPermanent">Permanent</th>
            <th class="hidden-xs" data-toggle="tooltip" data-placement="top" title="@LicenseMaintStrings.HelpExpiryDate">Expires</th>
            <th class="hidden-xs" data-toggle="tooltip" data-placement="top" title="@LicenseMaintStrings.HelpLastChanged">Last Changed</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="@(item.IsExpired ? "danger" : "")">
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.LicenseID, controller="Licenses" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.LicenseID, controller = "Licenses" })
                </td>
                <td class="hidden-xs">
                    @Html.DisplayFor(modelItem => item.LicenseID)
                </td>
                <td title="@item.MachineID" class="hidden-xs">
                    @Html.DisplayFor(modelItem => item.ShortMachineID)
                    <span style="display:none">@item.MachineID</span>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MachineName)
                </td>
                <td class="hidden-xs">
                    @Html.DisplayFor(modelItem => item.TotalUserCount)
                </td>
                <td class="hidden-xs">
                    @Html.DisplayFor(modelItem => item.IsPermanent)
                </td>
                <td class="hidden-xs">@(item.IsPermanent ? "-" : item.ExpiryDate.ToShortDateString())</td>
                <td class="hidden-xs">@item.ChangeTime.ToShortDateString()</td>
            </tr>
        }
    </tbody>
</table>